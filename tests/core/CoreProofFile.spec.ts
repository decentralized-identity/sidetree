describe('CoreProofFile', async () => {

  xit('Anchor file must have a link to a core proof file if it contains a recover or deactivate operation.', async () => {
  });

  xit('Anchor file must have a link to a provisional proof file if it contains an update operation.', async () => {
  });

  describe('createBuffer()', async () => {
  });
});
